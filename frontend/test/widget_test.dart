// Placeholder test. Replaced the default Flutter counter scaffolding (which
// referenced a non-existent `MyApp`) with a trivial test that just verifies
// the test runner is wired. Real widget tests can be added on top.

import 'package:flutter_test/flutter_test.dart';

void main() {
  test('test runner is wired', () {
    expect(1 + 1, equals(2));
  });
}
