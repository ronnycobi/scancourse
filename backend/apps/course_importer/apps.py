from django.apps import AppConfig


class CourseImporterConfig(AppConfig):
    default_auto_field = 'django.db.models.BigAutoField'
    name = 'apps.course_importer'
    verbose_name = 'Course Importer'
